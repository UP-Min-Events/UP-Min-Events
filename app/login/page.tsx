import Ops from './ops'
import Link from 'next/link'

export const metadata = {
    title: 'Events',
}

export default function Auth(){
    return (
        <div>
                <Ops />
        </div>
    )
}